@model List<Testimonial>

@{
    int counter = 0;
}
<div class="main-content">
    <div class="page-content">
        <div class="container-fluid">
            <h1>Testimonials</h1>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">No</th>
                        <th scope="col">Title</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Testimonial testimonial in Model)
                    {
                        counter++;
                        <tr>
                            <th scope="row">@counter</th>
                            <td>@testimonial.Title</td>
                            <td>
                                <a asp-action="update" asp-controller="testimonial" asp-route-id="@testimonial.Id" class="btn btn-outline-primary">Update</a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>